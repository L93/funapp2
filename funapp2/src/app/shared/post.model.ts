export interface PostInterface {
    id: string;
    name: string;
    description: string;
    created: string;
    rating: string;
}

// why does interface use ';' instead of ',' for its key: value
// obj?
